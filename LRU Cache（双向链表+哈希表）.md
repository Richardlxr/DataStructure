### 第一部分：为什么我们需要“缝合怪”？

#### 1. 哈希表的短板
你刚才手写的 `MyHashMap`，查找速度是 $O(1)$。
**但是！** 它是无序的。数据像撒胡椒面一样散落在内存里。
如果缓存满了（比如容量限制 3 个），你要删掉“最近最少使用（Least Recently Used）”的那个元素。
**请问：谁是最近最少使用的？**
哈希表一脸茫然：“我只知道 key 在哪，我哪知道谁最老？”
你想找到最老的元素，必须遍历所有桶，耗时 $O(N)$。**工业级缓存不能接受 $O(N)$。**

#### 2. 双向链表的短板
链表天生就有顺序。我们可以规定：
*   **链表头部（Head）**：放最新访问过的数据（热乎的）。
*   **链表尾部（Tail）**：放最久没碰过的数据（冷透的）。
当缓存满时，直接把 Tail 指向的节点砍掉（$O(1)$），多爽！
**但是！** 如果我要读取 key=5 的数据，链表是个瞎子，只能从头遍历到尾，耗时 $O(N)$。**工业级缓存也不能接受 $O(N)$。**

#### 3. 终极方案：哈希表 + 双向链表
我们要结合两者的优点：
1.  **哈希表（Map）**：充当“索引”，负责在 $O(1)$ 时间内找到节点在内存里的地址。
2.  **双向链表（List）**：充当“时间轴”，负责维护谁新谁旧。

**物理结构图（请在脑海中立刻构建）：**
*   内存里漂浮着一些 **Node** 节点。
*   这些节点被**双向指针（prev/next）** 串成一条龙。
*   旁边挂着一个 **哈希表**，Key 是数据的 Key，**Value 是指向那个 Node 的指针！**

---

### 第二部分：重新定义 Node（物理零件升级）

在 `MyHashMap` 里，你的 Node 只有 `next`。
在 LRU Cache 里，为了能随时把中间的节点“抠”出来移到头部，我们需要**双向指针**。

拿出草稿纸，写下新的结构体定义：

```cpp
struct DLinkedNode {
    int key;
    int value;
    DLinkedNode* prev;
    DLinkedNode* next;
    
    // 构造函数初始化
    DLinkedNode(int k=0, int v=0) : key(k), value(v), prev(nullptr), next(nullptr) {}
};
```

**导师的灵魂拷问：**
注意到了吗？**为什么 Node 里除了 `value`，还必须存 `key`？**
（提示：当你缓存满了，需要删掉链表尾部的节点（Tail）时，你不仅要删链表，还要去哈希表里把对应的索引也删掉！如果 Node 里不存 Key，你怎么知道去哈希表里删谁？）

---

### 第三部分：LRU 的物理动作分解

我们将使用 `std::unordered_map<int, DLinkedNode*> cache` 作为索引，用我们手写的双向链表作为数据容器。

这里有两个极其关键的动作，必须用“防守型编程”的严谨度来设计。

#### 动作一：`get(key)` —— “举手示意外加换位置”
当用户调用 `get(key)` 时：
1.  **查索引**：去 `map` 里查。如果没找到，返回 -1。
2.  **物理位移（关键！）**：如果找到了（拿到 `Node* node`），说明这个数据刚刚被用了一次！它变“热”了！
    *   你必须立刻把这个 `node` 从链表的当前位置“抠”出来（断开 prev/next）。
    *   然后把它插到链表的**最头部**。
3.  返回 `node->value`。

#### 动作二：`put(key, value)` —— “新老交替”
1.  **查索引**：
    *   **如果 key 已存在**：修改 `node->value`，然后做一次“物理位移”（移到头部）。
    *   **如果 key 不存在**：
        *   创建一个新节点 `newNode`。
        *   把它插到链表**头部**。
        *   在 `map` 里登记：`map[key] = newNode`。
        *   **容量检查（Kill）**：如果此时 `map.size() > capacity`：
            *   找到链表的**尾部节点**（Tail）。
            *   在 `map` 里删掉它的 Key（这就是为什么 Node 要存 Key！）。
            *   在链表里物理删除这个 Tail 节点，释放内存。

---

### 现在的测验（不写代码，先画图）

我要确保你的物理连线逻辑是清晰的。请在草稿纸上推演以下过程，并**回答我的问题**。

**场景：**
*   LRU Cache 容量 `capacity = 2`。
*   你需要维护两个**虚拟哨兵节点**：`head`（虚头）和 `tail`（虚尾），这样操作链表更安全（你之前学过的 Dummy Head 技巧）。
*   初始状态：`head <-> tail`。

**操作序列：**
1.  `put(1, 1)`
2.  `put(2, 2)`
3.  `get(1)`  <-- **关键点！**
4.  `put(3, 3)` <-- **发生淘汰！**

**请回答：**
1.  在执行完第 3 步 `get(1)` 后，你的双向链表从头到尾的顺序是什么？（谁靠近 head，谁靠近 tail？）
2.  在执行第 4 步 `put(3, 3)` 时，**谁**被淘汰了？
3.  画出第 4 步完成后的最终内存图：`map` 里存了哪两个 Key？链表里的顺序是什么？

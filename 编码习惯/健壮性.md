
## 一、申请内存new的时候
```cpp
try {
    int* ptr = new int[1000];
} catch (const std::bad_alloc& e) {
    // 处理内存不足，如清理内存、退出或提示用户
    std::cerr << "Memory allocation failed: " << e.what() << std::endl;
}
```

# 二、基本类型
![[队列/队列#3.==C++ 底层类型军规==]]



# 三、std::move
![[队列/队列#3. `std move` 的暴力美学（所有权劫持）]]
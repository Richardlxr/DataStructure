# 数据结构：<% tp.file.title %>

## 1. 基本概念
> [!quote] 定义
> <% tp.file.cursor(1) %>

## 2. 逻辑结构与存储结构
- **逻辑结构**：<% tp.file.cursor(2) %>
- **存储实现**：<% tp.file.cursor(3) %>

## 3. 基本操作 (ADT)
- [ ] `Init()`: 初始化
- [ ] `Insert()`: 插入
- [ ] `Delete()`: 删除
- [ ] `Search()`: 查找

## 4. 特性总结
- <% tp.file.cursor(4) %>

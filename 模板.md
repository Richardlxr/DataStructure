# 一、模板函数（Function Template）

## 1️⃣ 最基础模板函数

```cpp
#include <iostream>
using namespace std;

template<typename T>
T add(T a, T b) {
    return a + b;
}

int main() {
    cout << add<int>(3, 5) << endl;      // 显式指定类型
    cout << add(3.2, 4.8) << endl;      // 自动推导
}
```

### 关键点

```cpp
template<typename T>
```

- `T` 是类型占位符
    
- 编译时生成具体函数（不是运行时）
    

---

## 2️⃣ 多类型模板参数

```cpp
template<typename T, typename U>
auto add(T a, U b) {
    return a + b;
}
```

C++14 起可以用 `auto` 自动推导返回值。

---

## 3️⃣ 带默认模板参数

```cpp
template<typename T = int>
T square(T x) {
    return x * x;
}
```

---

## 4️⃣ 模板特化（函数特化）

```cpp
#include <cstring>

template<typename T>
void print(T value) {
    cout << value << endl;
}

// 针对 const char* 特化
template<>
void print<const char*>(const char* value) {
    cout << "string: " << value << endl;
}
```

---

# 二、模板类（Class Template）

## 1️⃣ 最基础模板类

```cpp
#include <iostream>
using namespace std;

template<typename T>
class Box {
private:
    T value;

public:
    Box(T v) : value(v) {}

    T get() const {
        return value;
    }
};

int main() {
    Box<int> b1(10);
    Box<double> b2(3.14);

    cout << b1.get() << endl;
    cout << b2.get() << endl;
}
```

---

## 2️⃣ 多类型模板类

```cpp
template<typename T, typename U>
class Pair {
public:
    T first;
    U second;

    Pair(T a, U b) : first(a), second(b) {}
};
```

使用：

```cpp
Pair<int, string> p(1, "hello");
```

---

## 3️⃣ 类内函数写在外部（重要）

```cpp
template<typename T>
class MyClass {
public:
    void show();
};

// 注意：必须写 template<>
template<typename T>
void MyClass<T>::show() {
    cout << "Hello Template" << endl;
}
```

⚠️ 不能写成：

```cpp
void MyClass::show()   // ❌ 错误
```

---

## 4️⃣ 默认模板参数

```cpp
template<typename T = int>
class Data {
public:
    T value;
};
```

---

# 三、类模板特化

## 1️⃣ 完全特化

```cpp
template<typename T>
class Printer {
public:
    void print(T value) {
        cout << value << endl;
    }
};

// 特化 int
template<>
class Printer<int> {
public:
    void print(int value) {
        cout << "Integer: " << value << endl;
    }
};
```

---

## 2️⃣ 偏特化（Partial Specialization）

只适用于类模板：

```cpp
template<typename T>
class MyClass {
};

// 针对指针类型偏特化
template<typename T>
class MyClass<T*> {
};
```

---

# 四、非类型模板参数

```cpp
template<typename T, int size>
class Array {
private:
    T data[size];

public:
    T& operator[](int index) {
        return data[index];
    }
};

int main() {
    Array<int, 5> arr;
    arr[0] = 10;
}
```

这里：

```
int size
```

不是类型，而是常量参数。

---

# 五、模板 + 头文件注意事项（非常重要）

模板函数 / 模板类：

👉 **必须写在头文件里**

因为模板是在使用时实例化。

如果写成：

```cpp
// .h
template<typename T>
class Test;

// .cpp
template<typename T>
class Test { ... }
```

会链接错误。

# 性质
- 有穷性
- 确定性
- 可行性
- 输入
- 输出


#算法的时间/空间复杂度的计算
### 如何计算「时间复杂度」
时间复杂度看的是**代码的执行次数（基本操作的次数）**。

#### 几个符号

$$
\lim_{n \to \infty} \frac{f(n)}{g(n)}
$$

| 符号       | 含义                           | 极限结果   |
| -------- | ---------------------------- | ------ |
| $O$      | 不超过(上界)，**f 的增长速度最多和 g 一样快** | 有限     |
| $\Omega$ | 不低于(下界），f 的增长 **至少和 g 一样快**  | > 0    |
| $\Theta$ | 同阶                           | 常数 ≠ 0 |
| $o$      | 严格更小                         | 0      |

#### 1. 常数阶 $O(1)$
代码没有循环，或者循环次数是固定的（与输入规模 $n$ 无关）。
```python
def print_first_element(arr):
    print(arr[0])  # 执行 1 次
    print("Hello") # 执行 1 次
# 总计 2 次，常数项，记作 O(1)
```

#### 2. 线性阶 $O(n)$
有一层循环，循环次数随着 $n$ 的增大而线性增大。
```python
def print_all(n):
    for i in range(n):  # 循环 n 次
        print(i)
# 时间复杂度：O(n)
```

#### 3. 平方阶 $O(n^2)$
双层嵌套循环，内外层循环都与 $n$ 有关。
```python
def print_pairs(n):
    for i in range(n):        # 外层 n 次
        for j in range(n):    # 内层 n 次
            print(i, j)
# 总执行次数：n * n = n^2，时间复杂度：O(n^2)
```
*注：如果是三层嵌套，就是 $O(n^3)$。*

#### 4. 对数阶 $O(\log n)$
循环中的变量每次都**成倍缩减**（或成倍增加），最典型的就是二分查找。
```python
def logarithmic_example(n):
    i = 1
    while i < n:
        i = i * 2  # 每次翻倍，距离 n 越来越近
```
*解释：假设 $n=16$，$i$ 的变化是 1 -> 2 -> 4 -> 8 -> 16，只需执行 4 次。因为 $2^4 = 16$，所以执行次数是 $\log_2(n)$。忽略底数，记作 $O(\log n)$。*

#### 5. 线性对数阶 $O(n \log n)$
通常是将时间复杂度为 $O(\log n)$ 的代码，外层嵌套一个 $n$ 次的循环。（常见于快速排序、归并排序等高级排序算法）。


### 递归的时间复杂度分析

1️⃣ **展开递归（递归树 / 展开法）**  
2️⃣ **写出递推式（Recurrence Relation）**  
3️⃣ **用主定理（Master Theorem）求解**


---

#### 一、最直观的方法：递归展开（递归树）

核心思想：

> 一个递归函数 = 当前工作 + 子问题递归

例如经典例子：

```cpp
int f(int n){
    if(n == 1) return 1;
    return f(n-1) + 1;
}
```

分析：

每次递归只做：

- 一个常数操作 `+1`
    
- 调用 `f(n-1)`
    

所以时间复杂度满足

$$ 
T(n) = T(n-1) + O(1)  
$$

展开：

```
T(n)
= T(n-1) + c
= T(n-2) + 2c
= T(n-3) + 3c
...
= T(1) + (n-1)c
```

所以

$$  
T(n)=O(n)  
$$

**直觉理解：**

递归调用了 n 次。

---

#### 二、画递归树（非常重要）

再看一个更典型的例子：**斐波那契**

```cpp
int fib(int n){
    if(n <= 2) return 1;
    return fib(n-1) + fib(n-2);
}
```

递推式：

$$  
T(n)=T(n-1)+T(n-2)+O(1)  
$$

递归树：

```
                fib(n)
            /           \
       fib(n-1)        fib(n-2)
      /       \        /      \
 fib(n-2) fib(n-3) fib(n-3) fib(n-4)
```

可以发现：

很多重复计算。

递归树规模接近：

```
2^n
```

所以

$$  
T(n)=O(2^n)  
$$

这就是为什么**原始递归 Fibonacci 很慢**。

---

# 三、分治递归（最常见）

例如：

```cpp
void f(int n){
    if(n <= 1) return;
    f(n/2);
    f(n/2);
}
```

递推式：

$$  
T(n)=2T(n/2)+O(1)  
$$

递归树：

```
        f(n)
       /    \
   f(n/2)  f(n/2)
```

树高：

```
log₂n
```

每层成本：

```
2^k
```

最后一层：

```
≈ n
```

总成本：

```
1 + 2 + 4 + ... + n
≈ 2n
```

所以

$$  
T(n)=O(n)  
$$

---

# 四、再看一个数据结构常见例子

```cpp
void f(int n){
    if(n<=1) return;
    
    for(int i=0;i<n;i++)
        cout<<"x";

    f(n/2);
}
```

递推式：

$$  
T(n)=T(n/2)+O(n)  
$$

展开：

```
n
n/2
n/4
n/8
...
```

求和：

$$  
n + n/2 + n/4 + ...  
$$

这是一个几何级数：

$$  
= 2n  
$$

所以

$$  
T(n)=O(n)  
$$

---

#### 五、递归复杂度通用套路

看到递归题基本按 **4步走**：

##### ① 写递推式

```
T(n) = 子问题 + 当前工作
```

例如

```
T(n)=T(n-1)+1
T(n)=2T(n/2)+n
T(n)=T(n/2)+n
```

---

##### ② 看递归深度

比如

```
n → n-1 → n-2 → ... → 1
深度 = n
```

或者

```
n → n/2 → n/4 → ... → 1
深度 = logn
```

---

##### ③ 计算每层工作量

例如：

```
每层 = n
每层 = 1
每层 = 2^k
```

---

##### ④ 总和

```
总复杂度 = 每层 × 层数
```

---

# 六、一个数据结构考试常见总结

| 递归形式               | 复杂度      |
| ------------------ | -------- |
| T(n)=T(n−1)+O(1)   | O(n)     |
| T(n)=T(n−1)+O(n)   | O(n²)    |
| T(n)=2T(n/2)+O(1)  | O(n)     |
| T(n)=2T(n/2)+O(n)  | O(nlogn) |
| T(n)=T(n/2)+O(n)   | O(n)     |
| T(n)=T(n-1)+T(n-2) | O(2^n)   |

---

💡 **给你一个非常重要的理解：**

递归复杂度 =

```
递归调用次数 × 每次调用的工作量
```

---

### 算法复杂度分类：易性算法<(N³) / 顽性算法>(N³)

- **多项式时间**：渐进时间复杂度有多项式时间限界的算法

- **指数时间算法**：渐进时间复杂度有指数时间限界的算法

- **常见多项式时间复杂度的关系**：
  
  O(1) < O(logn) < O(n) < O(nlogn) < O(n²) < O(n³)

- **常见指数时间复杂度的关系**：
  
  O(2ⁿ) < O(n!) < O(nⁿ)

---

### 如何计算「空间复杂度」
空间复杂度看的是算法运行过程中**额外分配的内存空间**。

==注意：不包含程序所占空间、所处理的数据所占空间==

#### 1. 空间 $O(1)$
只申请了有限的几个变量，不随输入规模 $n$ 变化。
```python
def sum_array(arr):
    total = 0           # 额外申请了 1 个变量 total
    for num in arr:
        total += num
    return total
# 空间复杂度：O(1)
```

#### 2. 空间 $O(n)$
申请了一个一维数组、链表或哈希表，其大小与输入规模 $n$ 成正比。
```python
def create_array(n):
    new_arr = []        # 申请了一个空数组
    for i in range(n):
        new_arr.append(i) # 往数组里塞了 n 个元素
    return new_arr
# 空间复杂度：O(n)
```

#### 3. 空间 $O(n^2)$
申请了一个二维数组（矩阵），长宽都和 $n$ 有关。
```python
def create_matrix(n):
    matrix = [[0] * n for _ in range(n)] # 生成 n * n 的二维数组
    return matrix
# 空间复杂度：O(n^2)
```

---

### 特殊情况 —— 「递归」的复杂度
#### 1. 递归的时间复杂度
**时间复杂度 = 递归树的节点总数 × 每个节点的时间**
*   **单分支递归**（如递归遍历数组）：一共递归 $n$ 次，每次操作 $O(1)$，总时间 $O(n)$。
*   **多分支递归**（如斐波那契数列 `f(n) = f(n-1) + f(n-2)`）：一分为二，二分为四，形成一颗二叉树。节点总数约为 $2^n$，时间复杂度是 **$O(2^n)$**（非常慢）。

#### 2. 递归的空间复杂度（重要！）

> [!important] 核心重点
> **知识点**：**空间复杂度 = 递归树的最大深度（调用栈的深度）**
> **理解关键**：
> - 因为递归的时候必须等上面的函数跑完了被弹出下面的函数帧栈才会跳出，整个挤压在一起。（若函数里面的空间复杂度不是O(1)，需要另算
> 

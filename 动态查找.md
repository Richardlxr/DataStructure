### 一、 二叉查找树（BST）知识点全景梳理

#### 1. 结构特性 (PPT 第 3 页)
*   **左小右大**：任何一个结点，==它的左子树里**所有**结点都比它小；右子树里**所有**结点都比它大。
*   ==**中序遍历必然升序**：这是检验你写的 BST 对不对的终极标准。

#### 2. 核心操作总结
*   **查找 (Find)**：从根开始，小往左走，大往右走。
*   **插入 (Insert)**：像寻宝一样往下走，走到 `NULL` 的地方，就是它的容身之所（永远作为叶子结点插入）。
*   **删除 (Remove)**（最复杂，PPT 第 10-16 页）：
    *   **情况 1：叶子结点** -> 直接删，父结点相应指针赋 `NULL`。
    *   **情况 2：只有一个孩子** -> 父结点直接接管这个唯一的孩子，然后删掉目标结点。
    *   **情况 3：有两个孩子** -> 找“替身”。一般找**右子树中的最小值**（即右子树一路向左走到底的结点）。把替身的数据拷贝过来，然后递归去右子树把那个真实的替身结点删掉。
> [!important] 核心重点
> **知识点**：注意不能`curr=min`（改变了树的结构）只能`curr->data=min->data`
> **理解关键**：
> - 关于指针的引用
> 
|核心问题|void foo(TreeNode* ptr) (纯指针)|void foo(TreeNode*& ptr) (指针的引用)|
|---|---|---|
|物理本质|拷贝了一份门牌号（栈上有新变量）|建立了一个通向原门牌号的通道（无新变量）|
|修改节点内部数据 (ptr->val = 1)|✅ 可以，拿着复印件也能找到真房子|✅ 可以|
|修改节点连接关系 (ptr->left = node)|✅ 可以，只要目标节点存在|✅ 可以|
|==改变指针变量自身的指向== (ptr = node)|❌ 失败，只改了子函数里的复印件|✅ 成功，直接修改了调用者的指针变量|

---

### 二、 硬核推导：BST 的性能分析（重点难点）
#### 1. 评价指标：ASL（Average Search Length，平均查找长度）
在内存中搜索，代价主要体现为“比较次数”。
计算公式：**ASL = ∑ ( 第 i 层结点的个数 × 层数 i ) / 总结点数 N** （假设等概率查找成功）

#### 2. 直观对比：最好情况 vs 最坏情况 (PPT 第 18 页)

*   **最好情况（接近完全二叉树）**：
    树很丰满（比如 PPT 左图：根50，第二层20,60，第三层15,30,70）。
    查找次数：1层的有1个，2层的有2个，3层的有3个。
    $ASL = \frac{1 \times 1 + 2 \times 2 + 3 \times 3}{6} = \frac{14}{6} \approx 2.33$
    此时性能接近 **$O(\log_2 n)$**。

*   **最坏情况（退化成链表）**：
    一直向左或一直向右长（比如 PPT 右图：15-20-30-50-60-70）。
    $ASL = \frac{1 + 2 + 3 + 4 + 5 + 6}{6} = \frac{21}{6} = 3.5$
    此时性能退化为 **$O(n)$**。

#### 3. 终极推导：平均情况性能数学推导 (PPT 第 19 页)

老师在 PPT 里抛出了一个复杂的公式和结论：$P(n) \le 1.38 \log_2 n$。这是怎么来的？

**【推导逻辑起航】**
设 $P(n)$ 为含有 $n$ 个结点的 BST 的**平均查找长度**。
假设输入序列是随机的，那么 $n$ 个结点中，任何一个结点都有可能是树根（共有 $n$ 种可能，每种概率为 $1/n$）。

假设恰好排名第 $i$ 小的结点做了树根（$i$ 的范围是从 $0$ 到 $n-1$）：
1.  **左子树**会有 $i$ 个结点。
2.  **右子树**会有 $n - i - 1$ 个结点。

**【计算特定一棵树的总比较次数】**
查找这棵树里所有结点的总比较次数 =
*   查找根结点：花费 1 次。
*   查找左子树所有结点：左子树自己在独立时的平均查找长度是 $P(i)$，总次数是 $i \times P(i)$。但现在它们在根结点下面，每次查找都要先经过根结点，所以每个结点的查找代价多加 1，左子树总代价为 $i \times (P(i) + 1)$。
*   同理，右子树所有结点的查找总代价为 $(n - i - 1) \times (P(n-i-1) + 1)$。

把上面的加起来，化简一下（注意 $1 + i + n - i - 1 = n$）：
**特定树的总代价** = $n + i \times P(i) + (n - i - 1) \times P(n-i-1)$

所以，这棵特定树的**平均查找长度**就是把它除以 $n$：
$ASL_{特定} = \frac{n + i \times P(i) + (n - i - 1) \times P(n-i-1)}{n}$
这正是 PPT 上那个复杂公式的分子部分重组后的样子！

**【综合所有情况】**
因为有 $n$ 种可能的树根，我们要求期望（再求和并乘以 $1/n$）：
$P(n) = \frac{1}{n} \sum_{i=0}^{n-1} \frac{n + i \times P(i) + (n - i - 1) \times P(n-i-1)}{n}$

由于左右子树的形态分布是对称的，$\sum i \times P(i)$ 和 $\sum (n-i-1) \times P(n-i-1)$ 的值其实是一样的，公式可以化简求解（这涉及到复杂的微积分和调和级数推导，属于算法导论级别）。

**【导师给你提炼的终极结论（必须记住）】**
经过推导，当 $n$ 很大时，调和级数展开后：
$P(n) \approx 2 \ln n = 2 \times (\ln 2 \times \log_2 n) \approx 2 \times 0.69 \times \log_2 n \approx \mathbf{1.38 \log_2 n}$


### 🌳 1. 为什么会有 AVL 树？（诞生的痛点）
普通二叉查找树（BST）有一个致命弱点：**它可能会“长歪”**（PPT P20）。
想象一下，如果你按从小到大的顺序往 BST 里插入 `1, 2, 3, 4, 5`，它不会长成一棵树，而是会变成一根向右倾斜的“糖葫芦”（也就是链表）。
这时候，你想找数字 5，原本期待树的查找速度是 $O(\log N)$，结果退化成了 $O(N)$。
**AVL树的使命**：就是加上一套“自动纠偏系统”，不管你怎么乱序插入，它都会在底层偷偷做手术（旋转），让树永远保持丰满，确保查找速度死死钉在 $O(\log N)$ 上！（PPT P24）

### ⚖️ 2. AVL 树如何判断自己歪了？（平衡因子与高度）
我们要让树保持平衡，但绝对的完美平衡（完全二叉树）要求太苛刻了，每次插入都要大规模重构，代价太大。所以 AVL 树采用了一种**“妥协的平衡”**（PPT P21-P22）。

*   **高度 (Height)**：PPT里规定，空树高度为 0，哪怕只有一个叶子节点，高度也是 1。
*   **平衡度 (Balance Factor)**：**左子树的高度 - 右子树的高度**。
*   **安全警戒线**：在 AVL 树中，任何一个节点的平衡度只能是 **`+1`、`0`、`-1`**。
*   **危机触发**：一旦某个节点的平衡度变成了 **`+2`** 或 **`-2`**，警报拉响，说明以这个节点为根的子树“失衡”了，它就是**“危机节点”**！

### 🌪️ 3. 四种危机情况与抢救手法（核心考点）
我们在树的某处插入了一个新节点，导致某个祖先节点失衡了。根据新节点插入的位置，总共只有 4 种情况（PPT P34）。

#### 💡 情况一：LL 型（左左太重） -> 需要 RR 旋转（向右单旋）
*   **画面感**：一个节点 A，你往它的**左孩子(B)的左子树**上挂了个重物，导致 A 向左严重倾斜。
*   **模型**：`/` 形状的倾斜。
*   **抢救**：拎起左孩子 B，把 A 往右边拽下来，当成 B 的右孩子。这就叫**向右单旋（PPT 代码里叫 LL 函数）**。


```
         (14) 爷爷 (t)  <-- 现任家主，头重脚轻
         /
       (9) 爸爸 (t1)
       /  \
     (5)  (10) 养子
    /
  孙子
```


```
       (14) 爷爷 (t)
       /
     (10) 养子被过继给爷爷了

   (9) 爸爸 (t1)  <-- 暂时脱离，准备篡位
   /
 (5) 
 /
孙子
```

```
          (9) 爸爸被提拔了！(t1)
          /   \
        (5)   (14) 爷爷被拽下去了！(t)
       /      /
     孙子   (10) 养子现在跟着爷爷
```


```
          (9) 新家主 (t)  <-- 完美平衡！
          /   \
        (5)   (14) 
       /      /
     孙子   (10) 
```

#### 💡 情况二：RR 型（右右太重） -> 需要 LL 旋转（向左单旋）
*   **画面感**：一个节点 A，你往它的**右孩子(B)的右子树**上挂了个重物，导致 A 向右严重倾斜。
*   **模型**：`\` 形状的倾斜。
*   **抢救**：拎起右孩子 B，把 A 往左边拽下来，当成 B 的左孩子。这就叫**向左单旋（PPT 代码里叫 RR 函数）**。


#### 💡 情况三：LR 型（左右折线） -> 需要 LR 双旋
*   **画面感**：节点 A 的左孩子是 B，新节点挂在了 **B 的右子树**上。
*   **模型**：`<` 形状的弯折（比如 A=14, B=5, 新节点=9）。
*   **为什么单旋不行？**：如果它是个“折线”，你直接单旋，它会换个方向继续打结，根本伸不直！
*   **抢救（PPT P42）**：分为两步。
    1.  先治下半截：对节点 B 执行向左单旋（RR），把 `<` 掰直成 `/`（变成纯粹的 LL 型）。
    2.  再治上半截：对节点 A 执行向右单旋（LL）。
    所以代码极其简单：`RR(t->left); LL(t);`

#### 💡 情况四：RL 型（右左折线） -> 需要 RL 双旋
*   **画面感**：节点 A 的右孩子是 B，新节点挂在了 **B 的左子树**上。
*   **模型**：`>` 形状的弯折。
*   **抢救（PPT P45）**：先对 B 向右单旋掰直成 `\`，再对 A 向左单旋。
    代码：`LL(t->right); RR(t);`

---

### 🧪 实验：如果对 LR 强行使用“单旋”会怎样？

我们在脑海里画一个典型的 **LR 型（`<` 形状的折线）**：
*   **爷爷 14**（由于左边太重，它是危机节点）
*   **爸爸 5**（14的左孩子）
*   **孙子 9**（5的右孩子，也就是新插入导致失衡的罪魁祸首）

```text
    (14) 爷爷  <-- 危机节点 (左重)
    /
  (5) 爸爸
    \
    (9) 孙子  <-- 导致折线的原因
```

现在，假如我们不懂 LR，我们看到爷爷 `14` 是左边重，就天真地照搬 **LL 旋转（向右拽）**。看看会发生什么：

1. 我们把 `14` 往右下拽。
2. `5` 被提拔成了新的根节点。
3. 但是等等！`5` 原本的右手里牵着孙子 `9`。现在 `5` 被提拔了，它的右手要用来牵拽下来的爷爷 `14`！
4. 那孙子 `9` 怎么办？根据规则，孙子 `9` 只能过继给爷爷 `14` 当左孩子。

**灾难发生了！** 我们来看看强行单旋后的结果：
```text
      (5)
         \
         (14)
         /
       (9)
```
发现了吗？**树根本没有变矮！** 原来是向左弯折，强行拽了一下之后，变成了向右弯折。高度一点没变，依然是失衡的！

---

### 🌟 破局：LR 为什么必须“先踹直，再拽倒”？

LR 的特殊之处就在于：**它像一条弯曲的腿（折线），如果你不先把它踹直，你是没办法把它整体放平的。**

所以，前辈们发明了**双旋**，也就是分两步走（PPT P42-43）：

#### 第一步：内部解决，把腿踹直（化 LR 为 LL）
既然问题出在爸爸 `5` 和孙子 `9` 这个折角上，我们就**先不管爷爷 `14`**，只对爸爸 `5` 动刀。
爸爸 `5` 的右边重（挂着9），我们就对爸爸执行一次 **RR旋转（向左单旋）**。
*动作*：把爸爸 `5` 向左下拽，提拔孙子 `9`。
*结果*：
```text
    (14) 爷爷
    /
  (9) 孙子被提拔了
  /
(5) 爸爸被拽下去了
```
**见证奇迹的时刻**：折线 `<` 变成了直线 `/`！LR 型被我们成功转化成了标准的 LL 型！

#### 第二步：全局解决，把直线拽平（标准的 LL 旋转）
现在这是一根笔直的 `/`，没有任何折线阻碍了。我们直接对爷爷 `14` 执行我们早就写好的 **LL旋转（向右单旋）**。
*动作*：把爷爷 `14` 向右下拽，提拔 `9`。
*结果*：
```text
      (9)
     /   \
   (5)   (14)
```
**完美平衡！**

---

### 💻 看看代码有多巧妙

理解了这个过程，你再看 PPT P46 的代码，会觉得有一种数学般的美感：

```cpp
// 治疗 LR (左孩子的右子树导致失衡)
void AvlTree<KEY, OTHER>::LR( AvlNode * & t ) {
    RR( t->left );  // 第一步：先对左孩子（爸爸）执行向左单旋，把腿踹直
    LL( t );        // 第二步：再对当前节点（爷爷）执行向右单旋，彻底摆平
}

// 治疗 RL (右孩子的左子树导致失衡)
// 完全对称的镜像操作
void AvlTree<KEY, OTHER>::RL( AvlNode * & t ) {
    LL( t->right ); // 第一步：先对右孩子执行向右单旋，踹直
    RR( t );        // 第二步：再对当前节点执行向左单旋，摆平
}
```




```cpp
template < class KEY, class OTHER >
    void AvlTree < KEY, OTHER > ::insert(const SET < KEY, OTHER > & x, AvlNode * & t) {
        if (t == NULL) //在空树上插入
            t = new AvlNode(x, NULL, NULL);
        else if (x.key < t - > data.key) { //在左子树上插入
            insert(x, t - > left);
            if (height(t - > left) - height(t - > right) == 2) //t失衡
                if (x.key < t - > left - > data.key) LL(t);
                else LR(t);
        } else if (t - > data.key < x.key) { //在右子树上插入
            insert(x, t - > right);
            if (height(t - > right) - height(t - > left) == 2) //t失衡
                if (t - > right - > data.key < x.key) RR(t);
                else RL(t);
        }
        //重新计算t的高度
        t - > height = max(height(t - > left), height(t - > right)) + 1; //调用树高的操作求左右子树高度
    }
```

1. **“递”的过程（往下走）：**
   * `t` 是**爷爷**，发现比自己小，调用 `insert(左孩子)`，爷爷挂起等待。
   * `t` 变成了**爸爸**，发现比自己小，调用 `insert(左孩子)`，爸爸挂起等待。
   * `t` 变成了**孙子**，发现比自己小，调用 `insert(左孩子)`，孙子挂起等待。
   * `t` 变成了 `NULL`，终于到底了！执行 `t = new AvlNode(...)`，新节点（重孙）诞生！然后 `return`。

2. **“归”的过程（往上回溯，这才是重点！）：**
   * 从 `NULL` 返回到了**孙子**层。此时 `t` 是**孙子**。代码继续往下走，**检查孙子有没有失衡**，重新计算孙子的高度。孙子层执行完毕，`return`。
   * 返回到了**爸爸**层。此时 `t` 是**爸爸**。代码继续往下走，**检查爸爸有没有失衡**，重新计算爸爸的高度。爸爸层执行完毕，`return`。
   * 返回到了**爷爷**层。此时 `t` 是**爷爷**。代码继续往下走，**检查爷爷有没有失衡**，重新计算爷爷的高度。

你看明白了吗？代码里虽然只写了一个 `if`，但因为它是写在递归调用**之后**的，所以随着递归的层层 `return`，**它实际上沿着新插入节点的来时路，自底向上（从下往上）依次检查了所有的祖先节点！**

### Non-recursion 

```cpp

```



### AVL 考试应试

### 🗡️ 必杀技 1：Insert (插入) 公式化流程

插入操作最简单，因为**只要发生一次旋转，整棵树就立刻恢复平衡**，不用继续往上查了。

**Step 1: 当作普通 BST 插入**

把新节点（孙子）按大小规规矩矩地插到叶子节点的位置。

**Step 2: 顺藤摸瓜找“失衡点”**

从刚插入的新节点开始，**往上**（向根节点方向）计算每个祖先节点的高度差（左边高度 - 右边高度）。

寻找**第一个**高度差绝对值变成 2 的节点。这就是我们要动刀的**爷爷节点**。

**Step 3: 确定旋转类型（看路径）**

站在爷爷节点上，往下看**通向新插入节点（孙子）的路径**，只看前两步：

- **左-左 (LL)：** 往左走了一步（爸爸），又往左走了一步（孙子）。$\rightarrow$ **对爷爷做右单旋**。
    
- **右-右 (RR)：** 往右走了一步（爸爸），又往右走了一步（孙子）。$\rightarrow$ **对爷爷做左单旋**。
    
- **左-右 (LR)：** 往左走了一步（爸爸），然后往右拐了（孙子）。$\rightarrow$ **先对爸爸左旋，再对爷爷右旋**。
    
- **右-左 (RL)：** 往右走了一步（爸爸），然后往左拐了（孙子）。$\rightarrow$ **先对爸爸右旋，再对爷爷左旋**。
    

**Step 4: 执行旋转（画图口诀）**

- **单旋 (LL/RR)：** “爸爸上位，爷爷被踩下去，爸爸内侧的腿（养子）扔给爷爷抱。”
    
- **双旋 (LR/RL)：** “孙子直接逆袭到最顶端，爸爸和爷爷分别变成孙子的左右护法，孙子原来的左右腿分别扔给爸爸和爷爷抱。”
    

_(插入结束，不用再往上看了！)_

---

### 🛡️ 必杀技 2：Remove (删除) 公式化流程

删除操作是考卷上的杀手题，因为它有**连锁反应**。你处理完一个失衡点，它的长辈可能又失衡了！

**Step 1: 当作普通 BST 删除**

- 叶子节点：直接抹掉。
    
- 只有一个孩子：孩子顶上来。
    
- **有两个孩子（高频考点）：** 去右子树找最小值（或者左子树最大值）当替身，把数字抄过来，然后去**把替身原来所在的节点删掉**。
    

**Step 2: 顺藤摸瓜找“失衡点”**

从**实际被物理删除的那个节点**（比如那个替身节点）的位置开始，往上计算高度差，找到第一个失衡的**爷爷节点**。

**Step 3: 确定旋转类型（看谁个子高）**

和插入不同，删除时没有那个“新节点”指路了。此时要看爷爷的**哪个孩子更高，哪个孙子更高**：

- 爷爷的**左**孩子高 $\rightarrow$ 左孩子是爸爸。
    
    - 爸爸的**左**脚长，或者**左右脚一样长** $\rightarrow$ **LL型**。
        
    - 爸爸的**右**脚长 $\rightarrow$ **LR型**。
        
- 爷爷的**右**孩子高 $\rightarrow$ 右孩子是爸爸。
    
    - 爸爸的**右**脚长，或者**左右脚一样长** $\rightarrow$ **RR型**。
        
    - 爸爸的**左**脚长 $\rightarrow$ **RL型**。
        

**Step 4: 执行旋转**

同插入的画图口诀。

**Step 5: 致命提醒（必须重复！）**

画完这一步的树之后，**必须继续往上检查祖先节点**，直到根节点！如果有新的失衡，重复 Step 3 和 Step 4。（这就是为什么删除操作的坑特别深）。




## AVL源码，细读注释

```cpp
#include <cstddef>

using namespace std;

template < class KEY, class OTHER > struct SET {
    KEY key;
    OTHER other;
    SET() {}
    SET(KEY k, OTHER o): key(k), other(o) {}
};

template < class KEY, class OTHER > class AvlTree {
    private:
        // 升级版节点：多了 height 属性
        struct AvlNode {
            SET < KEY, OTHER > data;
            AvlNode * left;
            AvlNode * right;
            int height; // 记录节点的高度

            AvlNode(const SET < KEY, OTHER > & element, AvlNode * lt = NULL, AvlNode * rt = NULL, int h = 1): data(element), left(lt), right(rt), height(h) {}
        };

    AvlNode * root;

    // 辅助函数：获取节点高度，防空指针
    int height(AvlNode * t) const {
        return t == NULL ? 0 : t - > height;
    }

    // 辅助函数：求最大值
    int max(int a, int b) {
        return (a > b) ? a : b;
    }

    // ==========================================
    // 🔥 你的任务：实现 LL 和 RR 单旋
    // ==========================================

    // Task 3.1: LL 旋转 (向右单旋) (参考 PPT P38)
    // t 传的是引用，旋转结束后，t 必须指向新的这棵子树的根节点
    void LL(AvlNode * & t) {
        // 1. 拎起 t 的左孩子，命名为 t1
        AvlNode * t1 = t - > left;

        // 2. 将 t1 的右孩子，过继给 t 当左孩子

        t - > left = t1 - > right;

        // 3. 把 t 拽下来，当成 t1 的右孩子

        t1 - > right = t;

        // 4. 重新计算高度（注意顺序！先算底下的人(t)，再算上面的人(t1)）
        t - > height = max(height(t - > left), height(t - > right)) + 1;
        t1 - > height = max(height(t1 - > left), height(t1 - > right)) + 1;

        // 5. 更新 t 的指向，让调用者知道谁是新的老大
        t = t1;
    }

    // Task 3.2: RR 旋转 (向左单旋) (参考 PPT P40)
    // 和 LL 是完全对称的镜像操作！
    void RR(AvlNode * & t) {
        AvlNode * t1 = t - > right;

        t - > right = t1 - > left;
        t1 - > left = t;

        t - > height = max(height(t - > left), height(t - > right)) + 1;

        t1 - > height = max(height(t1 - > left), height(t1 - > right)) + 1;

        t = t1;
    }

    void LR(AvlNode * & t) {
        RR(t - > left);
        LL(t);
    }

    void RL(AvlNode * & t) {
        LL(t - > right);
        RR(t);
    }

    void insert_recursion(const SET < KEY, OTHER > & x, AvlNode * & t) {
        if (t == nullptr) {
            t = new AvlNode(x);
        } else if (t - > data.key > x.key) {
            insert(x, t - > left);
            // 看t的左子树和右子树的高度来判断t是不是失衡
            if (height(t - > left) - height(t - > right) == 2) {
                if (x.key < t - > left - > data.key)
                    LL(t);
                else
                    LR(t);
            }
        } else if (t - > data.key < x.key) {
            insert(x, t - > right);
            if (height(t - > right) - height(t - > left) == 2) {
                if (x.key > t - > right - > data.key)
                    RR(t);
                else
                    RL(t);
            }
        }
        t - > height = max(height(t - > left), height(t - > right)) + 1;
    }

    // 私有的真正干活的 remove 函数，带有布尔返回值！
    // 返回 true：t高度没变，不需要调整；
    // 返回 false：t高度变矮了，必须向上级汇报。
    bool remove(const KEY & x, AvlNode * & t) {
        if (t == NULL)
            return true; // 没找到，也就是没删东西，高度当然没变，安全返回 true

        if (x < t - > data.key) {
            // TODO 1: 在左子树上删除
            // 递归调用 remove 去 t->left 里删。
            // 思考：如果左子树删完之后返回了 true，说明没变矮，我们当前这个节点 t 该返回什么？
            // 如果返回了 false，说明左边变矮了，我们需要调用 adjust(t, 0) 并返回它的结果。
            if (remove(x, t - > left))
                return true;
            else {
                // adjust(t, 0);
                return adjust(t, 0);
            }

        } else if (t - > data.key < x) {
            // TODO 2: 在右子树上删除
            // 和上面对称。如果是右边变矮了，调用 adjust(t, 1)。
            if (remove(x, t - > right))
                return false;
            else
                return adjust(t, 1);

        } else {
            // 找到了要删除的节点 t！(t->data.key == x)

            // 只要有两个孩子，我们就用“狸猫换太子”
            if (t - > left != NULL && t - > right != NULL) {
                // 找右子树的最小值当替身
                AvlNode * tmp = t - > right;
                while (tmp - > left != NULL)
                    tmp = tmp - > left;

                // 狸猫换太子
                t - > data = tmp - > data;

                // TODO 3: 去右子树把真正的替身删掉！
                // 记得这里要根据递归的返回值，决定是 return true 还是 return adjust(t, 1)！
                if (remove(t - > data.key, t - > right))
                    return true;
                else
                    return adjust(t, 1);

            } else {
                // 只有一个孩子，或者没有孩子
                AvlNode * oldNode = t;
                t = (t - > left != NULL) ? t - > left : t - > right;
                delete oldNode;
                // TODO 4: 你直接被删掉了（或者被唯一的儿子顶替了），你这棵树变矮了吗？
                // 应该 return true 还是 return false？
                return false;
            }
        }
    }
    // 判断(删完之后！！！)t的需不需要调整。如果需要，就调整，然后返回t高度有没有变
    bool adjust(AvlNode * & t, int subTree) {
        if (subTree == 1) {
            // ==========================================
            // 【右子树变矮了，左边可能会显得太重】
            // ==========================================

            // 情况 a：现在左边比右边高1。
            // 推出原来：左右一样高。高度没变。警报解除！
            if (height(t - > left) - height(t - > right) == 1) {
                return true;
            }

            // 情况 b：现在左右一样高。
            // 推出原来：右边比左边高1。现在树整体矮了1截。继续报警！
            if (height(t - > right) == height(t - > left)) {
                --t - > height;
                return false;
            }

            // 情况 c, d, e：现在左边比右边高 2 截了！塌房了，必须旋转！
            // 看左孩子(t->left)的脚是怎么长的：
            if (height(t - > left - > right) > height(t - > left - > left)) {
                // 情况 d：左孩子的“右脚”长，这是一个 `<` 折线 (LR型)
                LR(t);
                return false; // 双旋之后，整体高度必定变矮，向上报警
            } else {
                // 情况 c 和 e：左孩子“左脚”长 (LL型)，或者左右脚一样长
                LL(t);
                // 旋转完后，判断高度变了没（参考 PPT P52 Case 5）
                if (height(t - > right) == height(t - > left))
                    return false; // 依然变矮了
                else
                    return true; // 幸运地撑住了高度，警报解除
            }
        } else {
            // ==========================================
            // 【左子树变矮了，右边可能会显得太重】 (subTree == 0)
            // ==========================================
            // TODO: 请你基于上面极其严密的逻辑，完成这部分的“镜像翻译”！
            // 注意：高度相减的符号、LL/RR、LR/RL 统统都要反过来哦。

            // 如果删完之后两侧相同，说明原来左h比右h大了一，那么高度改变（注意维护高度！！！），但是不需要调整（没有失衡）
            if (height(t - > right) - height(t - > left) == 1) {
                t - > height--;
                return false;
            }

            // 如果删完之后右边比左边大1，说明整体高度不变，也不用调整
            if (height(t - > right) == height(t - > left) + 1)
                return true;

            // 如果删完之后右边比左边大2，说明要开始调整了（RR和RL里面自动维护了height，所以不用再动了）
            if (height(t - > right) == height(t - > left) + 2) {
                // 如果是右边的右边重（RR），注意一定是大于等于！！取等的时候树高不变
                if (height(t - > right - > right) >= height(t - > right - > left)) {
                    RR(t);
                    if (height(t - > right) == height(t - > left))
                        return false;
                    else
                        return true;

                } else {
                    RL(t);
                    return false;
                }
            }
        }
    }

    public: AvlTree() {
        root = NULL;
    }

    void insert(const SET < KEY, OTHER > & x) {
        AvlNode * & t = root;
        while (t != nullptr) {
            if (x.key < root - > data.key) {
                t = t - > left;
            } else if (x.key > root - > data.key) {
                t = t - > right;
            } else {
                return;
            }
        }
        t = new AvlNode(x);
    }
    // 对外公开的 remove 接口
    void remove(const KEY & x) {
        remove(x, root);
    }
};
```


# 实现细节


## 哨兵
- `data[0]`我们放成`std::numeric_limits<T>::min()`,(需要`#include <limits>`)，主要是为了代数方便

- 对于数组中任意一个下标为 

```
i
```

 的节点：

- 它的**左孩子**一定在数组的： 
    
    ```
    2×i
    ```
    
     位置
    
- 它的**右孩子**一定在数组的： 
    
    ```
    2×i+1
    ```
    
     位置
    
- 它的**父节点**一定在数组的： 
    
    ```
    ⌊i/2⌋
    ```
    
    （向下取整） 位置


## 入队

```cpp
    void enQueue(const T & x) {
        // 1. 如果空间满了，先扩容（哨兵占了一个空间）
        if (currentSize == capacity - 1) {
            doubleSpace();
        }

        // 2. 向上过滤 (Percolate up)
        // 首先在堆的末尾创建一个空穴 (hole)
        int hole = ++currentSize;

        // 3. 开始循环：
        // 条件1：hole > 1 (还没到根节点)
        // 条件2：x < array[hole / 2] (新元素比父亲还要小，不合规矩)
        // 步进：hole /= 2 (空穴跳到父亲的位置)
        for (; hole > 1 && x < array[hole / 2]; hole /= 2) {
            // 把父亲拉下来，填到当前的空穴里
            array[hole] = array[hole / 2];
        }

        // 4. 最后把新元素 x 填入最终找到的合适空穴中
        array[hole] = x;
    }
```

思想是洞一直往上浮动，但是我先不放，我就等到结束再放

## 出队（从上往下过滤）

```cpp
    T dequeue() {
        if (size == 0) {
            throw std::out_of_range("队列空了，没东西可出了！");
        }

        T minItem = data[1]; // 1. 记录那个真正最小的值（堆顶）
        T lastItem = data[size]; // 2. 拿到数组的最后一个元素
        size--; // 3. 堆的大小减 1

        if (size > 0) {
            percolateDown(1); // 4. 把 lastItem 放到坑位 1，并让它向下沉
        }

        return minItem; // 5. 返回最小值
    }

    private:
        // 向下过滤函数（核心！）
        // hole: 当前空穴的位置（一开始是 1）
        void percolateDown(int hole) {
            int child;
            T tmp = data[hole]; // 先把这个“要下沉”的元素存起来

            // 只要有左孩子，就继续尝试下沉
            for (; hole * 2 <= size; hole = child) {
                child = hole * 2;

                // 1. 选出较小的孩子
                if (child != size && data[child + 1] < data[child]) {
                    child++;
                }

                // 2. 如果孩子比 tmp 小，就把孩子提拔上来
                if (data[child] < tmp) {
                    data[hole] = data[child];
                } else {
                    // 3. 否则，tmp 找到了合适的位置
                    break;
                }
            }
            // 最后把 tmp 放入最终落脚的坑位
            data[hole] = tmp;
        }
```